import * as React from "react";

const Spacer = (props) => {
  return (
    <div
      style={{
        display: "block",
        height: props.height,
      }}
    ></div>
  );
};

export default Spacer;
